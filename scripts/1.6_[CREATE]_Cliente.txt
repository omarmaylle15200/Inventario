CREATE TABLE Cliente (
    IdCliente INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    IdTipoDocumento int,
    NumeroDocumento varchar(15),
    Nombre varchar(50),
    Apellidos varchar(100),
    Direccion text,
    Telefono varchar(20),
    Email varchar(100),
    EsActivo bit default 1,
    FechaRegistro TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FechaModificacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    IdUsuarioRegistro int,
    IdUsuarioModificacion int
);
ALTER TABLE Cliente ADD CONSTRAINT FK_Cliente_IdTipoDocumento FOREIGN KEY (IdTipoDocumento) REFERENCES TipoDocumento(IdTipoDocumento);
ALTER TABLE Cliente ADD CONSTRAINT FK_Cliente_IDUSUARIOREGISTRO FOREIGN KEY (IdUsuarioRegistro) REFERENCES usuario(IdUsuario);
ALTER TABLE Cliente ADD CONSTRAINT FK_Cliente_IDUSUARIOMODIFICACION FOREIGN KEY (IdUsuarioModificacion) REFERENCES usuario(IdUsuario);